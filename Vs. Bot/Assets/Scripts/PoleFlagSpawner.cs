using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FlagSpawner : MonoBehaviour {
    public GameObject bluepoleflag;
    public GameObject bluecarryflag;
    public GameObject redpoleflag;
    public GameObject redcarryflag;
    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {

        //if both flags do not exist
		//if (bluepoleflag. && bluecarryflag.){
            //spawn a new blue poleflag which is box flag on flagpole

       // }

        //if both flags do not exist
        //if (redpoleflag. && redcarryflag.){
            //spawn a new red poleflag which is box flag on flagpole

       // }

    }
}
